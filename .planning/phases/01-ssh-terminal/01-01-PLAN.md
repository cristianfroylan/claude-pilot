---
phase: "01-ssh-terminal"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/core/theme/app_theme.dart
  - android/app/src/main/AndroidManifest.xml
  - android/app/build.gradle
autonomous: true
requirements: []

must_haves:
  truths:
    - "flutter pub get resolves all dependencies without errors"
    - "flutter run builds and launches the app on Android without crashing"
    - "App shows the Machine List screen on first launch (empty state: 'No machines yet')"
    - "Dark Material 3 theme is applied — background is near-black, not white"
    - "Go_router routes are defined and /machines is the initial location"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies declared with correct versions"
      contains: "dartssh2"
    - path: "lib/main.dart"
      provides: "App entry point — ProviderScope wraps runApp"
      contains: "ProviderScope"
    - path: "lib/app.dart"
      provides: "MaterialApp.router + GoRouter config + ThemeData"
      contains: "MaterialApp.router"
    - path: "lib/core/theme/app_theme.dart"
      provides: "AppTheme.darkTheme + AppTheme.terminalTheme"
      contains: "useMaterial3: true"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Android hardening — allowBackup=false, adjustResize, INTERNET"
      contains: "allowBackup"
    - path: "android/app/build.gradle"
      provides: "Android SDK floor — minSdkVersion 23"
      contains: "minSdkVersion 23"
  key_links:
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "runApp(ProviderScope(child: ClaudePilotApp()))"
      pattern: "ClaudePilotApp"
    - from: "lib/app.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "theme: AppTheme.darkTheme"
      pattern: "AppTheme"
---

## Phase Goal

**As a** developer, **I want to** send prompts to Claude Code from my phone and see responses in a real ANSI terminal, **so that** I can work without being at my desk.

<objective>
Bootstrap the Flutter project skeleton: install all Phase 1 packages, configure Android hardening, define routing, and apply the Material 3 dark theme. After this plan, the app boots to an empty Machine List screen and every subsequent plan builds on a stable dependency foundation.

Purpose: Establish the project skeleton that all feature plans build on. Critical Android settings (allowBackup=false, minSdkVersion=23) must land before any device test to prevent credential loss and build failures.

Output: Working Flutter app that boots to Machine List screen with dark theme; all packages installed; Android hardened.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ssh-terminal/01-CONTEXT.md
@.planning/phases/01-ssh-terminal/01-RESEARCH.md
@.planning/phases/01-ssh-terminal/01-PATTERNS.md
@.planning/phases/01-ssh-terminal/01-UI-SPEC.md
@.planning/phases/01-ssh-terminal/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter project and install all Phase 1 packages</name>
  <files>pubspec.yaml</files>
  <read_first>
    - .planning/phases/01-ssh-terminal/01-RESEARCH.md (section "Standard Stack > Complete pubspec.yaml")
    - .planning/phases/01-ssh-terminal/01-PATTERNS.md (section "pubspec.yaml (config)")
    - CLAUDE.md (Technology Stack section — locked package versions)
  </read_first>
  <action>
    Run `flutter create claude_pilot --org com.example --platforms android,ios` in the project root (or confirm the project already exists and skip create). Then replace the generated pubspec.yaml with the exact content from RESEARCH.md "Complete pubspec.yaml" section:

    - name: claude_pilot
    - sdk: ^3.7.0, flutter: ">=3.38.0"
    - dependencies: dartssh2 ^2.18.0, xterm ^4.0.0, flutter_riverpod ^3.3.2, riverpod_annotation ^4.0.3, flutter_secure_storage ^10.3.1, shared_preferences ^2.5.5, go_router ^17.3.0
    - dev_dependencies: riverpod_generator ^4.0.4, build_runner (no version pin)
    - flutter: uses-material-design: true

    Run `flutter pub get` after writing pubspec.yaml. If `pub get` fails, inspect the error — most likely a version conflict with flutter_riverpod/riverpod_annotation pair. The versions above are from the verified pub.dev audit in RESEARCH.md (research date 2026-06-19 — if more than 30 days have passed, re-verify latest versions on pub.dev before pinning).

    Do NOT run `flutter pub upgrade` — use the exact versions from RESEARCH.md.
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && flutter pub get 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml exists and contains `dartssh2: ^2.18.0`
    - pubspec.yaml contains `xterm: ^4.0.0`
    - pubspec.yaml contains `flutter_riverpod: ^3.3.2`
    - pubspec.yaml contains `flutter_secure_storage: ^10.3.1`
    - pubspec.yaml contains `go_router: ^17.3.0`
    - `flutter pub get` exits with code 0 and prints "Got dependencies!"
    - No "version solving failed" message in pub get output
  </acceptance_criteria>
  <done>All Phase 1 packages installed and resolving; pubspec.yaml matches the RESEARCH.md specification.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Android manifest and build.gradle</name>
  <files>android/app/src/main/AndroidManifest.xml, android/app/build.gradle</files>
  <read_first>
    - .planning/phases/01-ssh-terminal/01-RESEARCH.md (sections "Code Examples > AndroidManifest.xml Required Changes" and "android/app/build.gradle Required Settings")
    - .planning/phases/01-ssh-terminal/01-PATTERNS.md (sections "AndroidManifest.xml (config)" and "android/app/build.gradle (config)")
    - The current android/app/src/main/AndroidManifest.xml (read actual file before modifying — it was generated by flutter create)
    - The current android/app/build.gradle (read actual file — structure varies by Flutter version)
  </read_first>
  <action>
    Edit AndroidManifest.xml to add or set these attributes before any device test — these are one-line changes that prevent catastrophic bugs:

    1. Add `<uses-permission android:name="android.permission.INTERNET"/>` as the first child of `<manifest>` — required for SSH over Wi-Fi.
    2. On the `<application>` tag: set `android:allowBackup="false"` — prevents flutter_secure_storage `InvalidKeyException` after Google Drive restore. If the tag already has `android:allowBackup="true"`, change it to false.
    3. On the `<activity>` tag: set `android:windowSoftInputMode="adjustResize"` — required for `resizeToAvoidBottomInset: true` on the terminal screen.

    Edit android/app/build.gradle to set:
    1. `compileSdkVersion 34` (or `compileSdk 34` depending on generated syntax)
    2. `minSdkVersion 23` — flutter_secure_storage v10+ enforces this; the Flutter-generated default is 21 which will cause a Gradle conflict
    3. `targetSdkVersion 34`

    Do NOT change the applicationId, versionCode, versionName, or any other settings.
  </action>
  <verify>
    <automated>grep -n "allowBackup" /home/cristian/Documentos/GitHub/claude-pilot/android/app/src/main/AndroidManifest.xml && grep -n "minSdkVersion" /home/cristian/Documentos/GitHub/claude-pilot/android/app/build.gradle</automated>
  </verify>
  <acceptance_criteria>
    - AndroidManifest.xml contains `android:allowBackup="false"` (not "true")
    - AndroidManifest.xml contains `android.permission.INTERNET`
    - AndroidManifest.xml contains `adjustResize` in windowSoftInputMode
    - build.gradle contains `minSdkVersion 23` (not 21 or lower)
    - build.gradle contains `compileSdkVersion 34` or `compileSdk 34`
    - `flutter build apk --debug` does NOT fail with "minSdkVersion X cannot be smaller than version Y"
  </acceptance_criteria>
  <done>Android manifest hardened against backup-key-loss; SDK floor set to 23 for flutter_secure_storage v10 compatibility.</done>
</task>

<task type="auto">
  <name>Task 3: Wire entry point, routing, and dark theme</name>
  <files>lib/main.dart, lib/app.dart, lib/core/theme/app_theme.dart, lib/features/machines/screens/machine_list_screen.dart</files>
  <read_first>
    - .planning/phases/01-ssh-terminal/01-PATTERNS.md (sections "lib/main.dart", "lib/app.dart", "lib/core/theme/app_theme.dart", "lib/features/machines/screens/machine_list_screen.dart")
    - .planning/phases/01-ssh-terminal/01-UI-SPEC.md (sections "Design System", "Color", "Screen 1: Machine List")
    - .planning/phases/01-ssh-terminal/01-CONTEXT.md (section "Implementation Decisions > App Navigation & Structure")
  </read_first>
  <action>
    Create the following files with the patterns from PATTERNS.md as the exact template:

    **lib/main.dart** — entry point:
    - `void main() { runApp(const ProviderScope(child: ClaudePilotApp())); }`
    - Import: `flutter_riverpod/flutter_riverpod.dart`, `app.dart`
    - No `WidgetsFlutterBinding.ensureInitialized()` unless async init is needed (it is not in this task)

    **lib/app.dart** — routing + theme:
    - `final _router = GoRouter(initialLocation: '/machines', routes: [...])` — define all four routes: `/machines`, `add`, `:id/edit`, `:id/terminal`
    - For `/machines/add` and `/machines/:id/edit`, use `AddEditMachineScreen` with optional `machineId` param
    - For `/machines/:id/terminal`, use `TerminalScreen(machineId: state.pathParameters['id']!)` — use a stub widget class for now (will be replaced in Plan 03)
    - `class ClaudePilotApp extends StatelessWidget` — returns `MaterialApp.router(theme: AppTheme.darkTheme, routerConfig: _router)`

    **lib/core/theme/app_theme.dart** — theme:
    - `AppTheme.darkTheme`: `ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF1E8BC3), brightness: Brightness.dark))`
    - `AppTheme.terminalTheme`: `TerminalTheme` constant with the full color palette from PATTERNS.md `app_theme.dart` section — background `Color(0xFF0F1117)`, foreground `Color(0xFFCDD6F4)`, cursor `Color(0xFF4BA3C7)`, plus all 16 ANSI colors. Import `package:xterm/xterm.dart` for `TerminalTheme`.

    **lib/features/machines/screens/machine_list_screen.dart** — stub screen (full implementation in Plan 02):
    - `class MachineListScreen extends ConsumerWidget` with a `Scaffold(appBar: AppBar(title: Text('Machines')), body: Center(child: Text('No machines yet')))`
    - This stub satisfies routing and app boot — it will be replaced fully in Plan 02

    Also create stub files for the other screen classes referenced by the router so the app compiles:
    - `lib/features/machines/screens/add_edit_machine_screen.dart` — stub ConsumerWidget returning `Scaffold(body: Center(child: Text('Add Machine')))`
    - `lib/features/terminal/screens/terminal_screen.dart` — stub ConsumerWidget taking `machineId` param, returning `Scaffold(body: Center(child: Text('Terminal: $machineId')))`

    Run `flutter analyze` after creating files — fix any import errors or type mismatches.
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && flutter analyze lib/main.dart lib/app.dart lib/core/theme/app_theme.dart 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/main.dart contains `ProviderScope`
    - lib/app.dart contains `MaterialApp.router`
    - lib/app.dart contains `initialLocation: '/machines'`
    - lib/app.dart defines routes for `/machines`, `add`, `:id/edit`, `:id/terminal`
    - lib/core/theme/app_theme.dart contains `useMaterial3: true`
    - lib/core/theme/app_theme.dart contains `Color(0xFF1E8BC3)` (seed color)
    - lib/core/theme/app_theme.dart contains `terminalTheme` with `Color(0xFF0F1117)` (terminal background)
    - `flutter analyze` reports zero errors (warnings acceptable)
    - `flutter build apk --debug` exits with code 0 (app compiles successfully)
  </acceptance_criteria>
  <done>App entry point wired, routing configured, dark theme applied. `flutter run` boots to Machine List stub screen with dark background.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device storage → app | flutter_secure_storage reads OS-protected credential store; no external boundary in this plan |
| internet | No network calls in Plan 01; INTERNET permission is declared but not exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | android:allowBackup | mitigate | Set to false in AndroidManifest.xml before any build — prevents Google Drive from copying encrypted blob without its decryption key |
| T-01-02 | Information Disclosure | Android Keystore key export | accept | Android Keystore keys are device-bound and non-exportable by design; no app-layer mitigation possible or needed |
| T-01-03 | Tampering | npm/pip/cargo installs | accept | Project uses pub.dev (Dart/Flutter) — all packages verified via pub.dev official API with verified publishers; no ASSUMED/SUS packages; legitimacy gate passed in RESEARCH.md |
| T-01-SC | Tampering | pub.dev package installs | mitigate | Package legitimacy verified in RESEARCH.md audit; all publishers confirmed (flutter.dev, terminal.studio, dash-overflow.net, juliansteenbakker) — no human checkpoint required |
</threat_model>

<verification>
After all tasks complete:

```bash
cd /home/cristian/Documentos/GitHub/claude-pilot
flutter pub get
flutter analyze
flutter build apk --debug
```

Expected: all three commands exit 0. The APK in `build/app/outputs/flutter-apk/app-debug.apk` proves the skeleton compiles.

Visual check on device: `flutter run` shows a dark screen titled "Machines" with "No machines yet" text.
</verification>

<success_criteria>
- `flutter pub get` succeeds — all Phase 1 packages installed
- `flutter analyze` reports zero errors
- `flutter build apk --debug` succeeds — app compiles for Android
- App boots to a dark-themed Machine List stub screen
- AndroidManifest.xml has `allowBackup="false"` and INTERNET permission
- build.gradle has `minSdkVersion 23`
- All four go_router routes defined in app.dart
</success_criteria>

<output>
Create `.planning/phases/01-ssh-terminal/01-01-SUMMARY.md` when done.
</output>
