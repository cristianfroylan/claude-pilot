---
phase: 04-reconexi-n-robusta
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - lib/features/terminal/widgets/reconnect_overlay.dart
  - lib/features/terminal/widgets/reconnect_banner.dart
  - lib/features/terminal/screens/terminal_screen.dart
autonomous: false
requirements: [RECON-01, RECON-02, RECON-03, RECON-04, RECON-05]
must_haves:
  truths:
    - "On initial connection failure the user sees an overlay with 'Attempt N/5 — retrying in Xs', a spinner, and a Cancel button, with terminal content visible behind it"
    - "On a mid-session drop an inline banner pinned to the top of the terminal shows 'Connection lost · Attempt N/3 · Retry in Xs' and a Cancel button, with scrollback fully visible below"
    - "When all retries are exhausted the user sees a Retry button that triggers one more connection attempt"
    - "After a successful reconnect a 'Reconnected' SnackBar appears for ~2s and the scrollback is unchanged"
    - "Cancel during retries stops the loop immediately"
  artifacts:
    - path: "lib/features/terminal/widgets/reconnect_overlay.dart"
      provides: "Initial-connect failure overlay (spinner + attempt/countdown copy + Cancel) and failed/Retry state"
      contains: "class ReconnectOverlay"
    - path: "lib/features/terminal/widgets/reconnect_banner.dart"
      provides: "Mid-session inline AnimatedContainer banner (attempt/countdown + Cancel)"
      contains: "class ReconnectBanner"
  key_links:
    - from: "lib/features/terminal/screens/terminal_screen.dart"
      to: "lib/features/terminal/providers/ssh_session_provider.dart"
      via: "cancel() and reconnect() notifier calls from overlay/banner buttons"
      pattern: "\\.notifier\\)\\.(cancel|reconnect)\\(\\)"
    - from: "lib/features/terminal/screens/terminal_screen.dart"
      to: "SshReconnecting -> SshConnected transition"
      via: "ref.listen fires Reconnected SnackBar"
      pattern: "SshReconnecting.*SshConnected|Reconnected"
---

<objective>
Build the reconnection UI: a full-screen overlay for initial-connect failure (and the exhausted/Retry state), an inline top-pinned banner for mid-session drops, and a post-reconnect SnackBar — all wired into `TerminalScreen` via a Stack so the terminal (and its scrollback) stays mounted underneath at all times.

Purpose: Surfaces the provider's reconnection state machine (Plan 02) to the user per the locked UI Feedback decisions in CONTEXT.md. This is the presentation tier of RECON-01..05; all logic already lives in the notifier. The Stack approach guarantees RECON-05: `TerminalViewWrapper` never unmounts, so xterm keeps its scrollback throughout.

Output: Two new widget files (`reconnect_overlay.dart`, `reconnect_banner.dart`) and a `terminal_screen.dart` body rebuilt as a Stack that switches on `SshSessionState`, plus a `ref.listen` that fires the "Reconnected" SnackBar on the SshReconnecting→SshConnected transition.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reconexi-n-robusta/04-CONTEXT.md
@.planning/phases/04-reconexi-n-robusta/04-RESEARCH.md

@lib/features/terminal/models/ssh_session_state.dart
@lib/features/terminal/screens/terminal_screen.dart
@lib/features/terminal/widgets/terminal_view_wrapper.dart

<interfaces>
<!-- Sealed states to render (from Plan 01): -->
<!-- SshConnecting(attempt, maxAttempts, secondsLeft) — initial overlay -->
<!-- SshConnected(terminal)                            — normal terminal -->
<!-- SshReconnecting(terminal, attempt, maxAttempts, secondsLeft) — banner over terminal -->
<!-- SshFailed(terminal)                               — Retry overlay over terminal -->

<!-- Notifier methods to call from buttons (from Plan 02): -->
<!-- ref.read(sshSessionProvider(machineId).notifier).cancel()    — RECON-03 -->
<!-- ref.read(sshSessionProvider(machineId).notifier).reconnect() — RECON-04 -->

<!-- Theme tokens (lib/core/theme/app_theme.dart): use Theme.of(context).colorScheme.*  -->
<!-- errorContainer/onErrorContainer for the mid-session banner; surface/primary for overlay -->

<!-- Exact UX copy (LOCKED, do not paraphrase): -->
<!-- Initial overlay:  "Attempt N/5 — retrying in Xs"  (em-dash, lowercase 's') -->
<!-- Mid-session banner: "Connection lost · Attempt N/3 · Retry in Xs" (middle-dot separators) -->
<!-- Post-reconnect SnackBar: "Reconnected", duration 2s -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReconnectOverlay and ReconnectBanner widgets</name>
  <read_first>
    - .planning/phases/04-reconexi-n-robusta/04-CONTEXT.md (UI Feedback — locked overlay/banner copy and layout)
    - .planning/phases/04-reconexi-n-robusta/04-RESEARCH.md (Pattern 6 + AnimatedContainer banner code example)
    - lib/core/theme/app_theme.dart (existing color tokens — no new design tokens)
    - lib/features/terminal/models/ssh_session_state.dart (field names to display)
  </read_first>
  <files>lib/features/terminal/widgets/reconnect_overlay.dart, lib/features/terminal/widgets/reconnect_banner.dart</files>
  <action>
    Create `lib/features/terminal/widgets/reconnect_overlay.dart` with a stateless `ReconnectOverlay` widget. Constructor params: `final int attempt`, `final int maxAttempts`, `final int secondsLeft`, `final VoidCallback onCancel`. It renders a full-screen semi-transparent scrim (`Container(color: Theme.of(context).colorScheme.surface.withValues(alpha: 0.85))`) centered content: a `CircularProgressIndicator`, then `Text('Attempt $attempt/$maxAttempts — retrying in ${secondsLeft}s')` using the LOCKED copy (em-dash `—`, lowercase `s`), then a `TextButton`/`OutlinedButton` labeled 'Cancel' calling `onCancel`. When `secondsLeft <= 0`, show 'Attempt $attempt/$maxAttempts — connecting…' instead of the countdown line (avoids "retrying in 0s"). Style with existing `AppTheme`/`colorScheme` tokens only — no new design constants.

    In the SAME file (or a second widget), add a stateless `ReconnectFailedOverlay` widget. Constructor params: `final VoidCallback onRetry`. Renders the same scrim with centered content: an error icon (`Icons.cloud_off`), `Text('Connection lost')`, a short subtitle `Text('All retry attempts failed.')`, and a `FilledButton` labeled 'Retry' calling `onRetry` (RECON-04). Keeping both overlays in one file is fine since they share the scrim style; if the file exceeds ~120 lines, that is acceptable.

    Create `lib/features/terminal/widgets/reconnect_banner.dart` with a stateless `ReconnectBanner` widget. Constructor params: `final int attempt`, `final int maxAttempts`, `final int secondsLeft`, `final VoidCallback onCancel`. Render an `AnimatedContainer(duration: const Duration(milliseconds: 200))` with `color: Theme.of(context).colorScheme.errorContainer`, a fixed compact height (~44), padded `Row` containing: a small `SizedBox(width:14,height:14, child: CircularProgressIndicator(strokeWidth: 2))`, a `Text` using LOCKED copy `'Connection lost · Attempt $attempt/$maxAttempts · Retry in ${secondsLeft}s'` (middle-dot `·` separators) colored `onErrorContainer`, a `Spacer`, and a `TextButton` labeled 'Cancel' calling `onCancel` (RECON-03). When `secondsLeft <= 0`, show `'Connection lost · Attempt $attempt/$maxAttempts · Reconnecting…'`. The banner itself is the always-on bar; the Plan-02 Stack positions it; this widget does not need to animate height to 0 (it is only mounted while SshReconnecting). Use `Material`/`SafeArea(top:true,bottom:false)` wrapping if needed so it does not collide with the status bar.
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && dart analyze lib/features/terminal/widgets/reconnect_overlay.dart lib/features/terminal/widgets/reconnect_banner.dart 2>&1 | grep -iE 'error -' ; test $? -ne 0 && echo "WIDGETS_NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class ReconnectOverlay" lib/features/terminal/widgets/reconnect_overlay.dart` returns 1.
    - `grep -c "class ReconnectFailedOverlay" lib/features/terminal/widgets/reconnect_overlay.dart` returns 1.
    - `grep -c "class ReconnectBanner" lib/features/terminal/widgets/reconnect_banner.dart` returns 1.
    - The overlay contains the LOCKED initial copy: `grep -c "Attempt \$attempt/\$maxAttempts — retrying in" lib/features/terminal/widgets/reconnect_overlay.dart` returns 1 (em-dash present).
    - The banner contains the LOCKED mid-session copy with middle dots: `grep -c "Connection lost · Attempt \$attempt/\$maxAttempts · Retry in" lib/features/terminal/widgets/reconnect_banner.dart` returns 1.
    - `grep -c "onCancel" lib/features/terminal/widgets/reconnect_banner.dart` returns at least 2 (param + invocation); `grep -c "onRetry" lib/features/terminal/widgets/reconnect_overlay.dart` returns at least 2.
    - `dart analyze` on both widget files reports no errors.
  </acceptance_criteria>
  <done>Three presentation widgets exist (ReconnectOverlay, ReconnectFailedOverlay, ReconnectBanner) using the exact locked UX copy and only existing theme tokens, with Cancel/Retry callbacks wired through constructor params; both files analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Stack, overlays, banner, and Reconnected SnackBar into TerminalScreen</name>
  <read_first>
    - lib/features/terminal/screens/terminal_screen.dart (current body after Plan 02 — the switch on sessionAsync.value and the ref.listen)
    - lib/features/terminal/widgets/reconnect_overlay.dart (ReconnectOverlay + ReconnectFailedOverlay APIs)
    - lib/features/terminal/widgets/reconnect_banner.dart (ReconnectBanner API)
    - .planning/phases/04-reconexi-n-robusta/04-RESEARCH.md (Pattern 6 Stack switch + SnackBar ref.listen example)
  </read_first>
  <files>lib/features/terminal/screens/terminal_screen.dart</files>
  <action>
    Modify `terminal_screen.dart` to render the reconnection UI over a persistent terminal. Import the two new widget files.

    Replace the body `Expanded` child (the switch added in Plan 02) with a `Stack` whose layers, bottom to top, are:
    1. Always-present terminal layer: render `TerminalViewWrapper(key: ValueKey(keyboardHeight), machineId: machineId, terminal: terminal)` for any state that carries a terminal — `SshConnected(:final terminal)`, `SshReconnecting(:final terminal)`, `SshFailed(:final terminal)`. For `SshConnecting()`/`null`, render `const Center(child: CircularProgressIndicator())` as the base layer (no terminal exists yet). Use a `switch` expression on `sessionAsync.value` to pick the base child. This keeps the SAME terminal instance mounted across SshConnected↔SshReconnecting↔SshFailed (RECON-05).
    2. Mid-session banner layer: `if (sessionAsync.value case SshReconnecting(:final attempt, :final maxAttempts, :final secondsLeft))` wrap a `Positioned(top: 0, left: 0, right: 0, child: ReconnectBanner(attempt: attempt, maxAttempts: maxAttempts, secondsLeft: secondsLeft, onCancel: () => ref.read(sshSessionProvider(machineId).notifier).cancel()))`.
    3. Initial-connect overlay layer: `if (sessionAsync.value case SshConnecting(:final attempt, :final maxAttempts, :final secondsLeft))` add `ReconnectOverlay(attempt: attempt, maxAttempts: maxAttempts, secondsLeft: secondsLeft, onCancel: () => ref.read(sshSessionProvider(machineId).notifier).cancel())`.
    4. Failed/Retry overlay layer: `if (sessionAsync.value is SshFailed)` add `ReconnectFailedOverlay(onRetry: () => ref.read(sshSessionProvider(machineId).notifier).reconnect())`.

    Update the `ref.listen` block (Plan 02 left a minimal SshFailed SnackBar): add the post-reconnect SnackBar per the locked decision — when `prevState is SshReconnecting && nextState is SshConnected`, call `ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Reconnected'), duration: Duration(seconds: 2)))`. This MUST NOT write to the terminal (would contaminate Claude Code scrollback). Keep the existing `SshFailed` SnackBar logic. Do not re-add any AlertDialog.

    The `TerminalViewWrapper` must remain mounted across the SshConnected→SshReconnecting→SshConnected cycle. Because the same `_terminal` instance flows through every variant, and the Stack always renders the terminal layer keyed only by `keyboardHeight`, the widget is preserved — verify the key does NOT include session state (only keyboardHeight) so reconnection does not force a remount that would reset xterm.
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && dart analyze lib 2>&1 | grep -iE 'error -' ; test $? -ne 0 && echo "PROJECT_NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "ReconnectOverlay\(" lib/features/terminal/screens/terminal_screen.dart` returns 1.
    - `grep -cE "ReconnectBanner\(" lib/features/terminal/screens/terminal_screen.dart` returns 1.
    - `grep -cE "ReconnectFailedOverlay\(" lib/features/terminal/screens/terminal_screen.dart` returns 1.
    - `grep -cE "\.notifier\)\.cancel\(\)" lib/features/terminal/screens/terminal_screen.dart` returns at least 2 (overlay + banner).
    - `grep -cE "\.notifier\)\.reconnect\(\)" lib/features/terminal/screens/terminal_screen.dart` returns 1.
    - `grep -c "Reconnected" lib/features/terminal/screens/terminal_screen.dart` returns 1 and appears inside a SnackBar with `Duration(seconds: 2)`: `grep -c "Duration(seconds: 2)" lib/features/terminal/screens/terminal_screen.dart` returns at least 1.
    - `grep -c "AlertDialog" lib/features/terminal/screens/terminal_screen.dart` returns 0.
    - `grep -c "Stack(" lib/features/terminal/screens/terminal_screen.dart` returns at least 1.
    - Whole-project gate: `dart analyze lib 2>&1 | grep -c 'error -'` returns 0.
  </acceptance_criteria>
  <done>TerminalScreen renders a Stack with the terminal always at the bottom and the overlay/banner/failed layers conditionally on top; Cancel calls cancel(), Retry calls reconnect(), the Reconnected SnackBar fires on SshReconnecting→SshConnected, and `dart analyze lib` reports zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete reconnection experience, end to end:
    - Initial-connect failure: full-screen overlay "Attempt N/5 — retrying in Xs" + spinner + Cancel, terminal area visible behind.
    - Mid-session drop: inline top banner "Connection lost · Attempt N/3 · Retry in Xs" + Cancel, scrollback fully visible below.
    - Exhausted retries: "Retry" button that triggers one more attempt.
    - Successful reconnect: "Reconnected" SnackBar (2s), scrollback unchanged.
    - Cancel stops retries immediately.
  </what-built>
  <how-to-verify>
    1. `cd /home/cristian/Documentos/GitHub/claude-pilot && flutter run` on a device/emulator on the same LAN as a configured machine.
    2. RECON-01 (initial failure): add or pick a machine with a WRONG port (e.g. 2222) or stop sshd, tap it. Confirm the overlay shows "Attempt 1/5 — retrying in Xs", the counter advances (2/5, 3/5…), and the seconds count down. Confirm a Cancel button is visible.
    3. RECON-03 (cancel): during a countdown, tap Cancel. Confirm retries stop immediately and you land on the Retry screen.
    4. RECON-04 (manual retry): tap Retry. Confirm it makes one more attempt.
    5. Connect successfully to a real machine, run `claude` or any command so the terminal has visible scrollback. Then drop the connection mid-session (disable Wi-Fi briefly, or `sudo systemctl restart sshd` on the host). Confirm the inline banner appears at the top showing "Connection lost · Attempt N/3 · Retry in Xs" and the prior scrollback REMAINS visible below it (RECON-02, RECON-05).
    6. Re-enable the network. Confirm a "Reconnected" SnackBar appears for ~2s and the terminal scrollback is exactly as before — nothing cleared (RECON-05).
    7. Confirm no AlertDialog pops up at any point and the app never crashes on disconnect.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (wrong copy, scrollback cleared, banner not showing, crash, etc.)</resume-signal>
</task>

</tasks>

<verification>
- `dart analyze lib` reports zero errors.
- Manual verification confirms all five RECON success criteria from ROADMAP.md Phase 4.
</verification>

<success_criteria>
The user sees visible, accurate reconnection progress for both initial and mid-session failures, can cancel and manually retry, and the xterm scrollback survives the entire reconnect cycle unchanged — fully satisfying RECON-01 through RECON-05.
</success_criteria>

<output>
Create `.planning/phases/04-reconexi-n-robusta/04-03-SUMMARY.md` when done.
</output>
