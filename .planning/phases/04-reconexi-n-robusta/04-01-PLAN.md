---
phase: 04-reconexi-n-robusta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/terminal/models/ssh_session_state.dart
autonomous: true
requirements: [RECON-01, RECON-02, RECON-04, RECON-05]
must_haves:
  truths:
    - "A sealed SshSessionState type exists with four exhaustive variants"
    - "Every variant that represents a connected-or-later state carries the xterm Terminal instance"
  artifacts:
    - path: "lib/features/terminal/models/ssh_session_state.dart"
      provides: "Sealed class SshSessionState + SshConnecting/SshConnected/SshReconnecting/SshFailed variants"
      contains: "sealed class SshSessionState"
      min_lines: 40
  key_links:
    - from: "lib/features/terminal/models/ssh_session_state.dart"
      to: "package:xterm/xterm.dart"
      via: "import for Terminal type"
      pattern: "import 'package:xterm/xterm.dart'"
---

<objective>
Define the `SshSessionState` sealed class — the contract every Phase 4 component depends on. This is a pure new file with no consumers yet; it is the interface that the provider refactor (Plan 02) and UI (Plan 03) implement against.

Purpose: A Dart 3 sealed class lets the UI exhaustively pattern-match on connection lifecycle states while always carrying a live `Terminal` instance from the point of first connection onward — this is the structural foundation that makes RECON-05 (scrollback preservation) possible. The state lives inside `AsyncData(...)`, never `AsyncLoading`/`AsyncError` after first connect, so the `TerminalView` widget never unmounts.

Output: `lib/features/terminal/models/ssh_session_state.dart` with one sealed class and four concrete variants.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reconexi-n-robusta/04-CONTEXT.md
@.planning/phases/04-reconexi-n-robusta/04-RESEARCH.md

<interfaces>
<!-- The Terminal type this model file imports. From package:xterm/xterm.dart. -->
<!-- Terminal is constructed elsewhere as: Terminal(maxLines: 2000) -->
<!-- This file only references the type Terminal; it never constructs one. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the SshSessionState sealed class and four variants</name>
  <read_first>
    - .planning/phases/04-reconexi-n-robusta/04-RESEARCH.md (Pattern 1: SshSessionState Sealed Class — exact field shapes)
    - .planning/phases/04-reconexi-n-robusta/04-CONTEXT.md (Reconnection State Model — locked variant list)
    - lib/features/terminal/providers/ssh_session_provider.dart (current import of package:xterm/xterm.dart and Terminal usage)
  </read_first>
  <files>lib/features/terminal/models/ssh_session_state.dart</files>
  <action>
    Create a new file `lib/features/terminal/models/ssh_session_state.dart`. Import `package:xterm/xterm.dart` for the `Terminal` type. Declare a `sealed class SshSessionState` with a `const SshSessionState();` constructor. Declare exactly four final classes extending it, matching the locked decision in CONTEXT.md "Reconnection State Model":
    - `SshConnecting` — fields `final int attempt`, `final int maxAttempts`, `final int secondsLeft`. Const constructor with all three as required named params. Does NOT carry a Terminal (pre-connection state).
    - `SshConnected` — field `final Terminal terminal`. Const positional constructor `const SshConnected(this.terminal)`.
    - `SshReconnecting` — fields `final Terminal terminal`, `final int attempt`, `final int maxAttempts`, `final int secondsLeft`. Const constructor with all four as required named params.
    - `SshFailed` — field `final Terminal terminal`. Const positional constructor `const SshFailed(this.terminal)`.
    Add a dartdoc comment above each variant stating its role per RESEARCH.md Pattern 1 (SshConnecting = initial attempt no terminal yet; SshConnected = live; SshReconnecting = mid-session drop, terminal kept alive; SshFailed = all retries exhausted, terminal kept alive for manual retry). Do not add `copyWith`, `==`, or `toString` overrides — they are not required by any consumer (UI reads fields directly via pattern matching). The three variants that carry a Terminal (SshConnected, SshReconnecting, SshFailed) MUST expose it under the field name `terminal` so the Plan 03 Stack switch can destructure `(:final terminal)`.
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && dart analyze lib/features/terminal/models/ssh_session_state.dart 2>&1 | grep -v '^Analyzing' | grep -iE 'error' ; test $? -ne 0 && echo "NO_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/features/terminal/models/ssh_session_state.dart` exists.
    - `grep -c "sealed class SshSessionState" lib/features/terminal/models/ssh_session_state.dart` returns 1.
    - `grep -cE "class SshConnecting|class SshConnected|class SshReconnecting|class SshFailed" lib/features/terminal/models/ssh_session_state.dart` returns 4.
    - `grep -c "final Terminal terminal" lib/features/terminal/models/ssh_session_state.dart` returns 3 (SshConnected, SshReconnecting, SshFailed carry it).
    - `grep -c "import 'package:xterm/xterm.dart'" lib/features/terminal/models/ssh_session_state.dart` returns 1.
    - `dart analyze lib/features/terminal/models/ssh_session_state.dart` reports no errors.
  </acceptance_criteria>
  <done>The sealed class compiles with zero analyzer errors and exposes all four variants with the exact field names required by downstream plans.</done>
</task>

</tasks>

<verification>
- `dart analyze lib/features/terminal/models/ssh_session_state.dart` passes with no errors.
- The four variants and their field signatures match RESEARCH.md Pattern 1 exactly.
</verification>

<success_criteria>
The sealed-class contract is committed and downstream plans can import `SshSessionState` and pattern-match on its four variants without any further definition work.
</success_criteria>

<output>
Create `.planning/phases/04-reconexi-n-robusta/04-01-SUMMARY.md` when done.
</output>
