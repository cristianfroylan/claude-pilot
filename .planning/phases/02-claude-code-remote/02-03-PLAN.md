---
phase: 02-claude-code-remote
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - pubspec.yaml
  - android/app/src/main/AndroidManifest.xml
  - lib/features/terminal/widgets/voice_bottom_sheet.dart
  - lib/features/terminal/widgets/input_bar.dart
autonomous: false
requirements: [VOZ-01, VOZ-02, VOZ-03, VOZ-04]
user_setup:
  - service: android-speech
    why: "Voice dictation requires the device's system speech recognizer (Google app / GMS) and runtime microphone permission. On a device without a speech service the mic button self-hides (VOZ-04) — this is correct behavior, not a failure."
    dashboard_config:
      - task: "Grant microphone permission when the OS prompts on first mic tap (physical device or emulator with Google app)"
        location: "Android system permission dialog"
must_haves:
  truths:
    - "The android_intent_plus → speech_to_text package substitution is human-approved before any install"
    - "speech_to_text ^7.4.0 is added to pubspec only after its legitimacy is human-verified"
    - "AndroidManifest declares RECORD_AUDIO and a RecognitionService <queries> intent"
    - "User taps the mic button, dictates, and the final transcript appears in a review bottom sheet"
    - "The bottom sheet has Send and Discard actions; voice never auto-submits — Send is explicit"
    - "When speech recognition is unavailable, the mic button is hidden with no error shown"
  artifacts:
    - path: "pubspec.yaml"
      provides: "speech_to_text dependency"
      contains: "speech_to_text:"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "RECORD_AUDIO permission + RecognitionService query"
      contains: "RECORD_AUDIO"
    - path: "lib/features/terminal/widgets/voice_bottom_sheet.dart"
      provides: "Read-only transcript review sheet with Send/Discard"
      contains: "class VoiceBottomSheet"
    - path: "lib/features/terminal/widgets/input_bar.dart"
      provides: "Mic IconButton + speech_to_text init/listen lifecycle"
      contains: "_voiceAvailable"
  key_links:
    - from: "lib/features/terminal/widgets/input_bar.dart"
      to: "SpeechToText.initialize"
      via: "initState _initSpeech sets _voiceAvailable"
      pattern: "_speech\\.initialize"
    - from: "lib/features/terminal/widgets/input_bar.dart"
      to: "VoiceBottomSheet"
      via: "showModalBottomSheet on finalResult"
      pattern: "VoiceBottomSheet"
    - from: "lib/features/terminal/widgets/voice_bottom_sheet.dart"
      to: "sshSessionProvider.notifier.sendText"
      via: "onSend callback"
      pattern: "onSend"
---

<objective>
Add voice dictation: a mic button in the InputBar that uses `speech_to_text` to capture spoken
input, presents the final transcript in a review bottom sheet, and sends it to the PTY only when
the user explicitly taps Send. When the device has no speech service, the mic button is hidden
with no error (VOZ-04).

This is a vertical slice: after this plan, a user can tap the mic, speak a prompt, review the
transcription, and send it to Claude Code — all without typing.

PACKAGE SUBSTITUTION (requires user approval — Task 1): CONTEXT.md locks `android_intent_plus`
for `ACTION_RECOGNIZE_SPEECH` and states "no speech_to_text package needed". RESEARCH.md found this
CANNOT be implemented as written — `android_intent_plus` has no `launchForResult` and cannot receive
the transcription back from the system speech dialog. The locked decision says "or equivalent"; the
documented equivalent is `speech_to_text` v7.4.0 (already the chosen voice package in CLAUDE.md).
Because this changes the locked package, Task 1 is a blocking decision checkpoint that presents the
substitution to the user and requires explicit approval BEFORE any install. This plan uses
`speech_to_text`, not `android_intent_plus`, only after that approval.

Purpose: Deliver VOZ-01..04 — dictate prompts by voice with explicit review-before-send.
Output: pubspec + manifest changes, a new VoiceBottomSheet widget, and mic wiring in InputBar.
Depends on Plan 02-01 because both modify `input_bar.dart` (sequential to avoid conflicts).

NOTE: `speech_to_text` is tagged `[ASSUMED]` in the RESEARCH Package Legitimacy Audit (slopcheck
unavailable). After the substitution is approved (Task 1), Task 2 is a blocking human-verify
legitimacy checkpoint that must pass before `flutter pub add`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-code-remote/02-CONTEXT.md
@.planning/phases/02-claude-code-remote/02-RESEARCH.md
@.planning/phases/02-claude-code-remote/02-PATTERNS.md
@.planning/phases/02-claude-code-remote/02-UI-SPEC.md

<interfaces>
<!-- Key contracts. Use these directly — no exploration needed. -->

speech_to_text 7.4.0 API (RESEARCH.md Pattern 3 + assumption A2):
```dart
import 'package:speech_to_text/speech_to_text.dart';
final _speech = SpeechToText();
final bool available = await _speech.initialize(onStatus: (_) {}, onError: (_) {});
await _speech.listen(
  onResult: (result) {
    // result.finalResult (bool), result.recognizedWords (String)
  },
  listenFor: const Duration(seconds: 30),
  pauseFor: const Duration(seconds: 5),
  localeId: null, // device default (Open Question 1 — Claude's Discretion)
);
_speech.stop();   // stop listening
_speech.cancel(); // in dispose()
// If `listen(listenFor:/pauseFor:)` top-level params fail to compile (A2),
// wrap them in SpeechListenOptions(...).
```

From lib/features/terminal/widgets/input_bar.dart (post Plan 02-01 — ConsumerStatefulWidget):
- `_InputBarState extends ConsumerState<InputBar>` with `bool _commandsVisible`
- main bar `Row`: `[Command toggle][Spacer][arrowBtn ←][↑][↓][→]` — mic IconButton inserts between `Spacer()` and the first `arrowBtn`
- `final isConnected = ref.watch(sshSessionProvider(widget.machineId)).hasValue;`
- `ref.read(sshSessionProvider(widget.machineId).notifier).sendText(text)` is how text reaches the PTY
- the file currently has NO initState/dispose — they must be added (analog: `_ConnectingDotState` in terminal_screen.dart lines 160-179)

VoiceBottomSheet contract (UI-SPEC section 3):
```dart
class VoiceBottomSheet extends StatelessWidget {
  final String transcript;
  final VoidCallback onSend;
  const VoiceBottomSheet({super.key, required this.transcript, required this.onSend});
}
```

AndroidManifest current state (the existing single <queries> block must be MERGED, not duplicated):
- `<uses-permission android:name="android.permission.INTERNET"/>` already present (do not duplicate)
- one existing `<queries>` block with the PROCESS_TEXT intent
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve android_intent_plus → speech_to_text package substitution</name>
  <decision>
    CONTEXT.md locks `android_intent_plus` for voice input via `ACTION_RECOGNIZE_SPEECH` and states
    "no speech_to_text package needed". This plan must substitute `speech_to_text` v7.4.0 instead.
    This checkpoint requires the user to approve the substitution before ANY package is installed.
  </decision>
  <context>
    - `android_intent_plus` can LAUNCH the Android `ACTION_RECOGNIZE_SPEECH` system dialog, but it has
      NO `launchForResult` / `startActivityForResult` equivalent — it cannot receive the transcribed
      text back from the system speech activity. The transcription would be lost. This makes the
      CONTEXT.md decision (as literally written) unimplementable for VOZ-01..03, which all require the
      app to receive and review the transcript before sending.
    - The CONTEXT.md decision explicitly says "`android_intent_plus` (or equivalent)" — `speech_to_text`
      v7.4.0 is the documented equivalent. It is already the chosen voice package in the project's
      CLAUDE.md (publisher csdcorp.com, verified), wraps the same on-device system ASR, and returns the
      final transcript via its `onResult` callback so the user can review before sending.
    - Behavioral outcome is identical to the CONTEXT.md intent: tap mic → system on-device speech →
      review transcript in a bottom sheet → explicit Send. No internet, no cloud STT (LAN-only
      constraint preserved). Only the implementing package changes.
  </context>
  <options>
    <option id="option-a">
      <name>Approve substitution: use speech_to_text v7.4.0</name>
      <pros>Implementable today; returns the transcript for review (VOZ-01..03); already vetted in CLAUDE.md; preserves LAN-only constraint.</pros>
      <cons>Deviates from the literal CONTEXT.md package name (but honors its "or equivalent" clause).</cons>
    </option>
    <option id="option-b">
      <name>Keep android_intent_plus</name>
      <pros>Matches the literal CONTEXT.md package name.</pros>
      <cons>Cannot return the transcript — VOZ-01..03 (review-before-send) become unimplementable. Blocks the phase goal.</cons>
    </option>
  </options>
  <resume-signal>Type "approved" to use speech_to_text (option-a), or "option-b" to halt and re-discuss.</resume-signal>
  <action>Do not run any install or edit any file. Present the substitution rationale above to the user: android_intent_plus has no launchForResult and cannot return the ACTION_RECOGNIZE_SPEECH result, so it cannot satisfy VOZ-01..03; speech_to_text v7.4.0 (already documented in CLAUDE.md, publisher csdcorp.com) is the documented equivalent. Proceed to Task 2 only after the user types "approved".</action>
  <verify>Human reviews the substitution rationale and types "approved" (or selects option-b to halt). No file was modified before approval.</verify>
  <done>User has explicitly approved substituting speech_to_text for the CONTEXT.md-locked android_intent_plus; no package was installed and no file was changed before approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify speech_to_text package legitimacy before install</name>
  <what-built>
    Nothing yet — this is a pre-install legitimacy gate. `speech_to_text` is tagged `[ASSUMED]`
    in the RESEARCH Package Legitimacy Audit because slopcheck was unavailable at research time.
    Per the package legitimacy gate, the install must be human-verified before it runs. This runs
    only after the substitution decision in Task 1 is approved.
  </what-built>
  <how-to-verify>
    1. Open https://pub.dev/packages/speech_to_text in a browser.
    2. Confirm: publisher is `csdcorp.com` (verified), latest version is `7.4.0` (or a compatible
       7.x), pub.dev "likes"/popularity is high, and the package has multi-year history.
    3. Confirm the source repo is github.com/csdcorp/speech_to_text and it is actively maintained.
    4. This package is already documented in the project's CLAUDE.md as the chosen voice package.
  </how-to-verify>
  <resume-signal>Type "approved" to allow `flutter pub add speech_to_text`, or describe a concern to halt.</resume-signal>
  <action>Do not run any install. Pause and present the legitimacy evidence above to the user. Verify on https://pub.dev/packages/speech_to_text that the publisher is csdcorp.com, the latest version is 7.x, and the source repo is github.com/csdcorp/speech_to_text. Proceed to Task 3 only after the user types "approved".</action>
  <verify>Human confirms the package is legitimate (publisher csdcorp.com, 7.x, active repo) and types "approved".</verify>
  <done>User has explicitly approved installing speech_to_text; no package was installed before approval.</done>
</task>

<task type="auto">
  <name>Task 3: Add speech_to_text dependency and AndroidManifest permissions</name>
  <files>pubspec.yaml, android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - pubspec.yaml (dependencies block lines 10-19 — match the existing caret-version style)
    - android/app/src/main/AndroidManifest.xml (line 3 uses-permission; the single <queries> block at lines 43-48 — MERGE into it, do not add a second <queries> element)
    - .planning/phases/02-claude-code-remote/02-RESEARCH.md (Pitfall 2: missing RECORD_AUDIO makes initialize() silently return false; Code Examples "AndroidManifest.xml additions")
    - .planning/phases/02-claude-code-remote/02-PATTERNS.md (sections "pubspec.yaml" and "AndroidManifest.xml")
  </read_first>
  <action>
    Run `flutter pub add speech_to_text` (this adds `speech_to_text: ^7.4.0` to pubspec.yaml and
    fetches it). Confirm the resolved version is 7.x.

    In android/app/src/main/AndroidManifest.xml:
    - Add `<uses-permission android:name="android.permission.RECORD_AUDIO"/>` next to the existing
      INTERNET permission, before `<application>`. Do NOT duplicate INTERNET (already present).
    - MERGE a new intent into the EXISTING `<queries>` block (do not create a second one):
      `<intent><action android:name="android.speech.RecognitionService" /></intent>`.

    Do NOT add `android_intent_plus` — it cannot return activity results (RESEARCH critical finding,
    confirmed by the Task 1 substitution decision).
    Do NOT add BLUETOOTH permissions (RESEARCH A4 — not required for device microphone in v1).
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && grep -c "speech_to_text:" pubspec.yaml && grep -c "RECORD_AUDIO" android/app/src/main/AndroidManifest.xml && grep -c "android.speech.RecognitionService" android/app/src/main/AndroidManifest.xml && test $(grep -c "<queries>" android/app/src/main/AndroidManifest.xml) -eq 1 && echo "single-queries-block"</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `speech_to_text:` with a 7.x constraint
    - AndroidManifest.xml contains exactly one `<queries>` element
    - AndroidManifest.xml contains `RECORD_AUDIO` and `android.speech.RecognitionService`
    - AndroidManifest.xml contains exactly one `INTERNET` permission (not duplicated)
    - `android_intent_plus` does NOT appear in pubspec.yaml
  </acceptance_criteria>
  <done>speech_to_text is installed; the manifest declares RECORD_AUDIO and the RecognitionService query in a single merged queries block.</done>
</task>

<task type="auto">
  <name>Task 4: Create VoiceBottomSheet review widget</name>
  <files>lib/features/terminal/widgets/voice_bottom_sheet.dart</files>
  <read_first>
    - lib/features/machines/screens/add_edit_machine_screen.dart (modal-with-confirm/cancel analog)
    - .planning/phases/02-claude-code-remote/02-UI-SPEC.md (section "3. Voice Review Bottom Sheet" — exact layout: drag handle, 'Review your message' heading w600, read-only transcript Container surfaceContainerHighest radius 8 padding 12, SelectableText fontSize 14 height 1.5, Row of [Discard TextButton][Send message FilledButton]; "Copywriting Contract" — 'Review your message' / 'Send message' / 'Discard')
    - .planning/phases/02-claude-code-remote/02-PATTERNS.md (section "voice_bottom_sheet.dart (CREATE)")
  </read_first>
  <action>
    Create `lib/features/terminal/widgets/voice_bottom_sheet.dart` as `class VoiceBottomSheet
    extends StatelessWidget` with `final String transcript;` and `final VoidCallback onSend;`.

    Build the UI-SPEC layout inside a `Padding(padding: EdgeInsets.fromLTRB(16, 8, 16, 16 +
    MediaQuery.of(context).viewInsets.bottom))` containing a `Column(mainAxisSize: MainAxisSize.min)`:
    - a centered drag handle (Container 32x4, radius 2, `colorScheme.onSurfaceVariant.withOpacity(0.4)`)
    - `Text('Review your message')` using `textTheme.bodyMedium` with FontWeight.w600, centered
    - a read-only transcript `Container(color: colorScheme.surfaceContainerHighest, padding:
      EdgeInsets.all(12), borderRadius: 8)` wrapping `SelectableText(transcript, style: TextStyle(
      fontSize: 14, height: 1.5))`
    - a `Row` of `Expanded(TextButton('Discard'))`, `SizedBox(width: 12)`, `Expanded(FilledButton(
      'Send message'))`.

    The Discard `TextButton.onPressed` calls `Navigator.of(context).pop()` (discards, sends nothing —
    VOZ-03). The Send `FilledButton.onPressed` calls `onSend()` (the caller is responsible for both
    sending text AND popping the sheet). All colors via `Theme.of(context).colorScheme` — no hardcoded
    values.
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && flutter analyze lib/features/terminal/widgets/voice_bottom_sheet.dart 2>&1 | grep -E "error|No issues"</automated>
  </verify>
  <acceptance_criteria>
    - voice_bottom_sheet.dart contains `class VoiceBottomSheet extends StatelessWidget` with `transcript` and `onSend` fields
    - Contains the literal heading `'Review your message'`, the Send label `'Send message'`, and the cancel label `'Discard'`
    - The transcript is shown read-only via `SelectableText`
    - Discard pops without calling onSend; Send calls `onSend`
    - `flutter analyze` on the file reports no errors
  </acceptance_criteria>
  <done>VoiceBottomSheet renders the transcript read-only with Send message / Discard actions; voice never auto-submits.</done>
</task>

<task type="auto">
  <name>Task 5: Wire mic button + speech_to_text lifecycle into InputBar</name>
  <files>lib/features/terminal/widgets/input_bar.dart</files>
  <read_first>
    - lib/features/terminal/widgets/input_bar.dart (post Plan 02-01 state — the main bar Row and the `_InputBarState` class; the mic IconButton inserts between `Spacer()` and the first `arrowBtn`)
    - lib/features/terminal/screens/terminal_screen.dart (lines 160-179 `_ConnectingDotState` — initState/dispose lifecycle template)
    - lib/features/terminal/widgets/voice_bottom_sheet.dart (the widget created in Task 4 — its `transcript`/`onSend` contract)
    - .planning/phases/02-claude-code-remote/02-RESEARCH.md (Pattern 3 speech_to_text integration; Pitfall 1 no android_intent_plus; Pitfall 6 gate on finalResult only + stop(); Pitfall 4 isScrollControlled; assumption A1 initialize() handles RECORD_AUDIO runtime request — if it does not, add an explicit permission request and note it in SUMMARY)
    - .planning/phases/02-claude-code-remote/02-UI-SPEC.md (section "1. InputBar — Extended Row" mic IconButton Icons.mic size 20, 36x36, Semantics 'Start voice input', tooltip 'Voice input'; hidden when !_voiceAvailable)
    - .planning/phases/02-claude-code-remote/02-PATTERNS.md (section "input_bar.dart" — _voiceAvailable state field, mic IconButton placement, initState/dispose)
  </read_first>
  <action>
    Add `import 'package:speech_to_text/speech_to_text.dart';` and an import for `voice_bottom_sheet.dart`.

    In `_InputBarState`: add fields `final _speech = SpeechToText();` and `bool _voiceAvailable = false;`.

    Add `initState` calling an async `_initSpeech()` that does `final available = await
    _speech.initialize(onStatus: (_) {}, onError: (_) {});` then `if (mounted) setState(() =>
    _voiceAvailable = available);`. If `initialize()` returns false (no service, or RECORD_AUDIO
    denied/undeclared per Pitfall 2), `_voiceAvailable` stays false → mic hidden (VOZ-04).

    Add `dispose` calling `_speech.cancel();` before `super.dispose();`.

    Add `_launchVoiceRecognition()`: guard `if (!_voiceAvailable || _speech.isListening) return;`,
    then `await _speech.listen(onResult: (result) { if (result.finalResult &&
    result.recognizedWords.isNotEmpty) { _speech.stop(); if (mounted)
    _showReviewSheet(result.recognizedWords); } }, listenFor: const Duration(seconds: 30), pauseFor:
    const Duration(seconds: 5), localeId: null);`. Gate on `finalResult` only and call `stop()` so
    the sheet shows once (Pitfall 6). Use `localeId: null` for device default (Open Question 1).

    Add `_showReviewSheet(String transcript)` calling `showModalBottomSheet<void>(context: context,
    isScrollControlled: true, builder: (_) => VoiceBottomSheet(transcript: transcript, onSend: () {
    ref.read(sshSessionProvider(widget.machineId).notifier).sendText('$transcript\n');
    Navigator.of(context).pop(); }))`. `isScrollControlled: true` is REQUIRED (Pitfall 4). The `\n`
    is appended on send (VOZ-02/VOZ-03 — explicit Send only).

    In the main bar Row, insert between `const Spacer()` and the first `arrowBtn`:
    `if (_voiceAvailable) Semantics(label: 'Start voice input', child: IconButton(tooltip: 'Voice
    input', icon: const Icon(Icons.mic, size: 20), onPressed: isConnected ? _launchVoiceRecognition :
    null))`. The mic is omitted entirely when `_voiceAvailable` is false (VOZ-04 — no placeholder, no
    error). Do not modify the command panel sections from Plan 02-01.
  </action>
  <verify>
    <automated>cd /home/cristian/Documentos/GitHub/claude-pilot && grep -c "_speech.initialize" lib/features/terminal/widgets/input_bar.dart && grep -c "if (_voiceAvailable)" lib/features/terminal/widgets/input_bar.dart && flutter analyze lib/features/terminal/widgets/input_bar.dart 2>&1 | grep -E "error|No issues"</automated>
  </verify>
  <acceptance_criteria>
    - input_bar.dart contains `final _speech = SpeechToText();` and `bool _voiceAvailable = false;`
    - input_bar.dart has `initState` calling `_speech.initialize` and `dispose` calling `_speech.cancel()`
    - `_launchVoiceRecognition` gates on `result.finalResult` and calls `_speech.stop()` before showing the sheet
    - `_showReviewSheet` uses `showModalBottomSheet` with `isScrollControlled: true` and only sends on the `onSend` callback (never auto-sends)
    - The mic `IconButton` is wrapped in `if (_voiceAvailable)` and a `Semantics(label: 'Start voice input')`
    - The command panel sections from Plan 02-01 are unchanged
    - `flutter analyze lib/features/terminal/widgets/input_bar.dart` reports no errors
  </acceptance_criteria>
  <done>Tapping the mic launches speech_to_text, the final transcript appears in the review sheet, Send pipes `transcript + '\n'` to the PTY, Discard sends nothing, and the mic is hidden when voice is unavailable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Microphone → app | Spoken audio captured by the OS speech recognizer, returned as transcript text |
| Transcript → PTY write | User-authored transcript sent to the user's own authenticated SSH session after explicit review |
| npm/pub install | `speech_to_text` package fetched from pub.dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | pub.dev install of speech_to_text ([ASSUMED]) | mitigate | Blocking decision checkpoint (Task 1) confirms the package substitution, then a blocking human-verify checkpoint (Task 2) confirms publisher csdcorp.com + pub.dev legitimacy before `flutter pub add`. Never auto-approvable. |
| T-02-07 | Tampering | Voice transcript injected into PTY | accept | Per RESEARCH Security Domain V5: the user reviews the transcript in the bottom sheet and must tap Send explicitly. Same threat model as typed input to one's own machine. |
| T-02-08 | Information Disclosure | Microphone access (RECORD_AUDIO) | mitigate | Permission declared in manifest and requested at runtime via the OS dialog; capture is OS-managed and only triggered by an explicit mic tap. No audio is stored or transmitted off-device by the app. |
| T-02-09 | Information Disclosure | initialize() false leaks no error (VOZ-04) | accept | Intentional: failure self-hides the mic button silently — no information disclosed, correct graceful-degradation behavior. |
</threat_model>

<verification>
- Task 1 decision checkpoint approved (substitution) before any install
- Task 2 legitimacy checkpoint approved before install
- `grep` confirms speech_to_text in pubspec, RECORD_AUDIO + RecognitionService in a single manifest queries block
- `flutter analyze` reports no errors across voice_bottom_sheet.dart and input_bar.dart
- Manual (covered by phase human-verify on a device with a speech service): tap mic → speak → final transcript appears in the sheet → tap Send message → text + newline reaches the terminal → repeat → tap Discard → nothing sent. On a device without a speech service: mic button is absent, no error.
</verification>

<success_criteria>
- android_intent_plus → speech_to_text substitution explicitly approved by the user (Task 1)
- speech_to_text installed only after legitimacy checkpoint (T-02-SC)
- Mic button captures voice, transcript shown for review, Send is explicit (VOZ-01, VOZ-02, VOZ-03)
- Mic button hidden with no error when voice unavailable (VOZ-04)
- `android_intent_plus` not used; `flutter analyze` clean
</success_criteria>

<output>
Create `.planning/phases/02-claude-code-remote/02-03-SUMMARY.md` when done
</output>
