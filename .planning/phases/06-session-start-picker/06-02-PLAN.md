---
phase: 06-session-start-picker
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - lib/features/terminal/widgets/session_picker_sheet.dart
  - lib/features/terminal/screens/terminal_screen.dart
autonomous: true
requirements:
  - PICK-01
  - PICK-03
  - PICK-04

must_haves:
  truths:
    - "After SSH connects (SshConnected), if machine has folders, the picker sheet appears exactly once per session lifecycle"
    - "Tapping a folder closes the sheet and sends cd <path>\\n to the SSH shell immediately"
    - "Tapping 'Start blank' closes the sheet and sends no command"
    - "If machine has no configured folders, no sheet appears and session starts blank"
    - "The picker sheet cannot be dismissed by back gesture or barrier tap"
    - "The picker does NOT reappear when mid-session reconnection succeeds (_pickerShown guard)"
  artifacts:
    - path: "lib/features/terminal/widgets/session_picker_sheet.dart"
      provides: "StatelessWidget picker bottom sheet — folderPaths list, onFolderSelected callback, 'Start blank' TextButton"
      contains: "SessionPickerSheet"
    - path: "lib/features/terminal/screens/terminal_screen.dart"
      provides: "ConsumerStatefulWidget conversion + _pickerShown guard + showModalBottomSheet trigger in ref.listen"
      contains: "_pickerShown"
  key_links:
    - from: "lib/features/terminal/screens/terminal_screen.dart"
      to: "lib/features/terminal/widgets/session_picker_sheet.dart"
      via: "showModalBottomSheet builder"
      pattern: "SessionPickerSheet"
    - from: "lib/features/terminal/screens/terminal_screen.dart"
      to: "ssh_session_provider.dart sendText()"
      via: "onFolderSelected callback → ref.read(sshSessionProvider(machineId).notifier).sendText('cd $path\\n')"
      pattern: "sendText.*cd.*\\\\n"
---

<objective>
Create the SessionPickerSheet widget and wire it into TerminalScreen as the post-connect project picker (Component B).

Purpose: PICK-01, PICK-03, PICK-04 — after SSH shell is ready, show the picker once if folders are configured. Folder tap sends `cd <path>\n`. No-folder machines go straight to blank session without any sheet.

Output:
- `session_picker_sheet.dart` — new StatelessWidget, modeled on VoiceBottomSheet
- `terminal_screen.dart` — converted from ConsumerWidget to ConsumerStatefulWidget; `_pickerShown` bool added; picker trigger appended to existing ref.listen block
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-session-start-picker/06-RESEARCH.md
@.planning/phases/06-session-start-picker/06-PATTERNS.md
@.planning/phases/06-session-start-picker/06-UI-SPEC.md
@.planning/phases/06-session-start-picker/06-01-SUMMARY.md

<interfaces>
<!-- Read these files before touching anything. -->

From lib/features/terminal/screens/terminal_screen.dart (current — ConsumerWidget):
  Line 24:  class TerminalScreen extends ConsumerWidget
  Line 26:  final String machineId;
  Line 30:  Widget build(BuildContext context, WidgetRef ref) {
  Lines 31–44: ref.watch calls for sessionAsync, permissionLine, machines, machine, machineName
  Lines 49–67: ref.listen block — existing snackbar logic for SshReconnecting→SshConnected and SshFailed
  Line 49:  ref.listen(sshSessionProvider(machineId), (prev, next) {
  Lines 53–59: ScaffoldMessenger.showSnackBar on reconnect
  Lines 62–66: ScaffoldMessenger.showSnackBar on SshFailed

  Key provider access (line 40–44):
    final machines = ref.watch(machineProvider).value;
    final machine = machines?.cast<dynamic>().firstWhere(
      (m) => m.id == machineId,
      orElse: () => null,
    );

  Imports in use:
    package:flutter/material.dart
    package:flutter_riverpod/flutter_riverpod.dart
    package:go_router/go_router.dart
    ../../machines/providers/machines_provider.dart
    ../models/ssh_session_state.dart
    ../providers/permission_detector_provider.dart
    ../providers/ssh_session_provider.dart
    ../widgets/input_bar.dart
    ../widgets/permission_card.dart
    ../widgets/reconnect_banner.dart
    ../widgets/reconnect_overlay.dart
    ../widgets/terminal_view_wrapper.dart

From lib/features/terminal/widgets/voice_bottom_sheet.dart (exact analog for new sheet):
  - StatelessWidget, no Riverpod imports
  - Constructor: transcript String, onSend VoidCallback (required named)
  - Drag handle: Container(width:32, height:4, color: colorScheme.onSurfaceVariant.withValues(alpha:0.4), borderRadius:2)
  - Padding: fromLTRB(16, 8, 16, 16 + viewInsets.bottom)
  - colorScheme via: final colorScheme = Theme.of(context).colorScheme;

From lib/providers/ssh_session_provider.dart (verified):
  sendText() signature: void sendText(String text)
  Note: sendText does NOT append \n — caller must pass 'cd $path\n'

From lib/features/machines/providers/machines_provider.dart (verified):
  Provider name: machineProvider (Riverpod generator drops 'Notifier' suffix)
  Used as ref.watch(machineProvider) and ref.read(machineProvider.notifier)

From lib/features/terminal/models/ssh_session_state.dart (verified):
  Sealed class variants: SshConnecting, SshReconnecting, SshConnected, SshFailed
  SshConnected carries: terminal (xterm Terminal instance)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionPickerSheet widget</name>
  <files>lib/features/terminal/widgets/session_picker_sheet.dart</files>
  <read_first>
    lib/features/terminal/widgets/voice_bottom_sheet.dart — this is the exact analog; copy the drag handle pattern, outer Padding, colorScheme access, and TextButton style exactly
    .planning/phases/06-session-start-picker/06-UI-SPEC.md — Component B section for exact spacing values, ListTile spec, and "Start blank" button spec
  </read_first>
  <action>
    Create a new file `lib/features/terminal/widgets/session_picker_sheet.dart`.

    It is a StatelessWidget (no Riverpod — callbacks are injected via constructor).

    Import only: `package:flutter/material.dart`

    CLASS SIGNATURE:
      class SessionPickerSheet extends StatelessWidget
      Constructor params (all required, named):
        - `List<String> folderPaths` — the list of paths to display
        - `void Function(String path) onFolderSelected` — called when user taps a folder

    BUILD METHOD:
      final colorScheme = Theme.of(context).colorScheme;

      Return: SafeArea wrapping a Column with mainAxisSize: MainAxisSize.min, containing:

      1. Drag handle (copy exactly from VoiceBottomSheet lines 34–43):
         Center(
           child: Container(
             width: 32, height: 4,
             decoration: BoxDecoration(
               color: colorScheme.onSurfaceVariant.withValues(alpha: 0.4),
               borderRadius: BorderRadius.circular(2),
             ),
           ),
         )

      2. SizedBox(height: 24) — lg spacing

      3. Sheet title (left-aligned, inside Padding(horizontal: 16)):
         Text('Choose a project',
           style: Theme.of(context).textTheme.bodyMedium?.copyWith(fontWeight: FontWeight.w600))

      4. SizedBox(height: 16)

      5. Folder list — ListView.separated inside Padding(horizontal: 16):
         - shrinkWrap: true
         - physics: NeverScrollableScrollPhysics() (parent Column provides unbounded — needs fixed content)
         - itemCount: folderPaths.length
         - separatorBuilder: SizedBox(height: 8)
         - itemBuilder: Semantics(label: 'Open session in ${folderPaths[index]}', child: ListTile(
             tileColor: colorScheme.surfaceContainerHighest,
             shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)),
             leading: Icon(Icons.folder_outlined, color: colorScheme.onSurfaceVariant),
             title: Text(folderPaths[index],
               style: const TextStyle(fontSize: 14),
               overflow: TextOverflow.ellipsis,
             ),
             onTap: () {
               Navigator.of(context).pop();           // pop FIRST (synchronous)
               onFolderSelected(folderPaths[index]);  // callback AFTER pop
             },
           ))

      6. Divider with Padding (8 dp top and bottom):
         Padding(padding: const EdgeInsets.symmetric(vertical: 8), child: const Divider())

      7. "Start blank" TextButton (full-width, left-aligned text):
         Padding(
           padding: EdgeInsets.fromLTRB(16, 0, 16, 32 + MediaQuery.of(context).padding.bottom),
           child: Align(
             alignment: Alignment.centerLeft,
             child: TextButton(
               onPressed: () => Navigator.of(context).pop(),
               style: TextButton.styleFrom(foregroundColor: colorScheme.onSurfaceVariant),
               child: const Text('Start blank'),
             ),
           ),
         )

    NOTE on sheet height: The sheet is shown via showModalBottomSheet without isScrollControlled,
    so Flutter's default 9/16 screen height cap applies. For machines with many folders the list
    scrolls — ListView.separated with NeverScrollableScrollPhysics inside a SingleChildScrollView
    wrapping the whole column is the correct structure. Wrap the whole Column body in
    SingleChildScrollView if folderPaths could be long (> ~5 items).

    Correct outer structure:
      return SafeArea(
        child: SingleChildScrollView(
          child: Padding(
            padding: const EdgeInsets.fromLTRB(0, 8, 0, 0),
            child: Column(
              mainAxisSize: MainAxisSize.min,
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                // drag handle (centered)
                // title
                // list (shrinkWrap, NeverScrollable, Padding horizontal 16)
                // divider
                // start blank button
              ],
            ),
          ),
        ),
      );

    Do NOT import flutter_riverpod — no providers in this widget.
    Do NOT add isScrollControlled to the showModalBottomSheet call (that happens in terminal_screen.dart, not here).
    Do NOT add a close/X button — the sheet is non-dismissible; only folder tap or "Start blank" exits.
  </action>
  <verify>
    flutter analyze lib/features/terminal/widgets/session_picker_sheet.dart
    Expected: no errors
    grep -c 'onFolderSelected' lib/features/terminal/widgets/session_picker_sheet.dart
    Expected: >= 2 (declaration + call)
    grep -c 'Navigator.of(context).pop()' lib/features/terminal/widgets/session_picker_sheet.dart
    Expected: 2 (one for folder tap, one for start blank)
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/features/terminal/widgets/session_picker_sheet.dart` exits 0, no errors
    - `grep 'class SessionPickerSheet extends StatelessWidget' lib/features/terminal/widgets/session_picker_sheet.dart` returns 1 match
    - `grep 'void Function(String path) onFolderSelected' lib/features/terminal/widgets/session_picker_sheet.dart` returns 1 match
    - `grep "Navigator.of(context).pop()" lib/features/terminal/widgets/session_picker_sheet.dart` returns exactly 2 matches
    - `grep "onFolderSelected(folderPaths\[index\])" lib/features/terminal/widgets/session_picker_sheet.dart` returns 1 match (called AFTER pop)
    - `grep "'Start blank'" lib/features/terminal/widgets/session_picker_sheet.dart` returns 1 match
    - `grep "isDismissible\|enableDrag" lib/features/terminal/widgets/session_picker_sheet.dart` returns 0 matches (those params belong in the caller, not here)
    - `grep "import 'package:flutter_riverpod" lib/features/terminal/widgets/session_picker_sheet.dart` returns 0 matches
  </acceptance_criteria>
  <done>SessionPickerSheet is a self-contained StatelessWidget with folder list, tap-to-select, and "Start blank" — no analysis errors</done>
</task>

<task type="auto">
  <name>Task 2: Convert TerminalScreen to ConsumerStatefulWidget and wire picker trigger</name>
  <files>lib/features/terminal/screens/terminal_screen.dart</files>
  <read_first>
    lib/features/terminal/screens/terminal_screen.dart — read the COMPLETE current file before any edit; the conversion is mechanical but every ref.watch, ref.listen, and local variable usage must be verified before and after
    lib/features/terminal/widgets/session_picker_sheet.dart — confirms the widget class name and constructor params set in Task 1
    .planning/phases/06-session-start-picker/06-RESEARCH.md — Pattern 3 (picker trigger), Pitfall 1 (reconnect guard), Pitfall 2 (addPostFrameCallback)
  </read_first>
  <action>
    This task has two parts: (A) mechanical ConsumerWidget → ConsumerStatefulWidget conversion,
    and (B) appending the picker trigger to the existing ref.listen block.

    PART A — ConsumerWidget → ConsumerStatefulWidget conversion:

    1. Change class declaration from:
         class TerminalScreen extends ConsumerWidget {
           final String machineId;
           const TerminalScreen({super.key, required this.machineId});
           @override
           Widget build(BuildContext context, WidgetRef ref) {
       to:
         class TerminalScreen extends ConsumerStatefulWidget {
           final String machineId;
           const TerminalScreen({super.key, required this.machineId});
           @override
           ConsumerState<TerminalScreen> createState() => _TerminalScreenState();
         }

         class _TerminalScreenState extends ConsumerState<TerminalScreen> {
           bool _pickerShown = false;

           @override
           Widget build(BuildContext context) {

    2. Update all `machineId` references in the State class body to `widget.machineId`.
       In ConsumerState, `machineId` is no longer a local variable — it must be `widget.machineId`.
       Search the entire build method and ref.listen block for bare `machineId` and add the `widget.` prefix.

    3. In ConsumerState, `ref` is inherited — remove the `WidgetRef ref` parameter from build().
       The call sites (ref.watch, ref.listen, ref.read) are unchanged syntactically.

    4. The `_ConnectingDot` private StatefulWidget at the bottom of the file is UNCHANGED.
       Do not touch it.

    PART B — Append picker trigger to existing ref.listen block:

    Inside the existing ref.listen(sshSessionProvider(widget.machineId), (prev, next) { ... }) block,
    after the existing SshFailed snackbar logic (after the closing `}` of the SshFailed block),
    add:

      // PICK-01 / PICK-04: Show folder picker on FIRST SshConnected transition only.
      // _pickerShown guard prevents re-showing on mid-session reconnect (Pitfall 1 in RESEARCH.md).
      if (!_pickerShown && next.value is SshConnected) {
        _pickerShown = true;
        final allMachines = ref.read(machineProvider).value;
        final pickerMachine = allMachines?.cast<dynamic>().firstWhere(
          (m) => m.id == widget.machineId,
          orElse: () => null,
        );
        final paths = pickerMachine?.folderPaths as List<String>?;
        if (paths != null && paths.isNotEmpty) {
          // addPostFrameCallback prevents "setState during build" assertion (Pitfall 2 in RESEARCH.md).
          WidgetsBinding.instance.addPostFrameCallback((_) {
            if (!mounted) return;
            showModalBottomSheet<void>(
              context: context,
              isDismissible: false,
              enableDrag: false,
              backgroundColor: Theme.of(context).colorScheme.surface,
              builder: (_) => SessionPickerSheet(
                folderPaths: paths,
                onFolderSelected: (path) {
                  ref
                      .read(sshSessionProvider(widget.machineId).notifier)
                      .sendText('cd $path\n');
                },
              ),
            );
          });
        }
      }

    ADD IMPORT for SessionPickerSheet at the top of the file, alongside other widget imports:
      import '../widgets/session_picker_sheet.dart';

    GUARD on PICK-04: the `if (paths != null && paths.isNotEmpty)` check means machines with no
    configured folders never call showModalBottomSheet. No separate code path needed.

    CRITICAL RULES:
    - Do NOT reset _pickerShown on reconnect — it must stay true for the lifetime of the ConsumerState instance.
    - Do NOT call sendText with 'cd $path' (no \n) — it must be 'cd $path\n'. sendText does not append \n (RESEARCH.md verified).
    - Do NOT wrap the picker trigger in `if (mounted)` before addPostFrameCallback — mounted check belongs INSIDE the callback, which the code above already does.
    - Do NOT use ref.watch inside addPostFrameCallback — use ref.read (callback fires after build).
    - Do NOT remove the existing SshReconnecting snackbar or SshFailed snackbar logic.
    - Do NOT change the _ConnectingDot widget at the bottom of the file.

    After all changes, verify the build method signature is:
      @override
      Widget build(BuildContext context) {   // no WidgetRef ref param
    and that the class ends with the closing `}` of _TerminalScreenState,
    followed by the unchanged _ConnectingDot StatefulWidget.
  </action>
  <verify>
    flutter analyze lib/features/terminal/screens/terminal_screen.dart
    Expected: no errors
    grep 'ConsumerStatefulWidget' lib/features/terminal/screens/terminal_screen.dart
    Expected: 1 match (class TerminalScreen extends ConsumerStatefulWidget)
    grep '_pickerShown' lib/features/terminal/screens/terminal_screen.dart
    Expected: >= 2 matches (declaration + usage in trigger)
    grep 'SessionPickerSheet' lib/features/terminal/screens/terminal_screen.dart
    Expected: >= 2 matches (import + instantiation)
    flutter analyze .
    Expected: no errors across entire project
  </verify>
  <acceptance_criteria>
    - `grep "class TerminalScreen extends ConsumerStatefulWidget" lib/features/terminal/screens/terminal_screen.dart` returns 1 match
    - `grep "class _TerminalScreenState extends ConsumerState<TerminalScreen>" lib/features/terminal/screens/terminal_screen.dart` returns 1 match
    - `grep "bool _pickerShown = false" lib/features/terminal/screens/terminal_screen.dart` returns 1 match
    - `grep "Widget build(BuildContext context) {" lib/features/terminal/screens/terminal_screen.dart` returns 1 match (no WidgetRef param)
    - `grep "widget.machineId" lib/features/terminal/screens/terminal_screen.dart` returns at least 4 matches (all former bare `machineId` references updated)
    - `grep "addPostFrameCallback" lib/features/terminal/screens/terminal_screen.dart` returns 1 match
    - `grep "isDismissible: false" lib/features/terminal/screens/terminal_screen.dart` returns 1 match
    - `grep "sendText.*cd.*\\\\n" lib/features/terminal/screens/terminal_screen.dart` returns 1 match (includes \n)
    - `grep "session_picker_sheet.dart" lib/features/terminal/screens/terminal_screen.dart` returns 1 match (import line)
    - `grep "class _ConnectingDot" lib/features/terminal/screens/terminal_screen.dart` returns 1 match (unchanged)
    - `flutter analyze .` exits 0 with no errors across the whole project
  </acceptance_criteria>
  <done>TerminalScreen is a ConsumerStatefulWidget; picker appears once after first SshConnected per session; folder tap sends cd command; no-folder machines skip the sheet; entire project has no analysis errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Machine.folderPaths → SSH shell stdin | Folder path strings configured by the user are sent via sendText() as `cd <path>\n` — no external input source; paths come from local storage written by the same user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | sendText('cd $path\n') | accept | Path values sourced from user's own local storage (shared_preferences); no remote or third-party injection vector; SSH server validates the path at execution time |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 6 — legitimacy gate not applicable (RESEARCH.md § Package Legitimacy Audit) |
</threat_model>

<verification>
1. `flutter analyze .` — zero errors across entire project
2. `grep 'ConsumerStatefulWidget' lib/features/terminal/screens/terminal_screen.dart` — returns 1 match
3. `grep '_pickerShown' lib/features/terminal/screens/terminal_screen.dart` — returns >= 2 matches
4. `grep "Navigator.of(context).pop()" lib/features/terminal/widgets/session_picker_sheet.dart` — returns 2 matches
5. Manual: machine with folders → connect → picker sheet appears, folder tap closes sheet and sends cd command visible in terminal
6. Manual: machine with no folders → connect → no sheet appears, terminal ready immediately
7. Manual: disconnect mid-session, reconnect → picker does NOT reappear
8. Manual: tap "Start blank" → sheet closes, no cd command sent, terminal at default shell directory
</verification>

<success_criteria>
- SessionPickerSheet is a clean StatelessWidget with no Riverpod imports; callbacks injected via constructor
- TerminalScreen converts cleanly to ConsumerStatefulWidget; no behavior change for existing features (reconnect, snackbars, overlay, permission card, InputBar all unchanged)
- _pickerShown guard fires once per ConsumerState instance lifetime
- PICK-01 satisfied: picker sheet appears after connect (if folders configured)
- PICK-03 satisfied: folder tap sends `cd <path>\n` to SSH shell
- PICK-04 satisfied: no-folder machines skip the sheet entirely
- Full project passes `flutter analyze .` with no errors
</success_criteria>

<output>
Create `.planning/phases/06-session-start-picker/06-02-SUMMARY.md` when done
</output>
