---
phase: 06-session-start-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/models/machine.dart
  - lib/features/machines/screens/add_edit_machine_screen.dart
autonomous: true
requirements:
  - PICK-02

must_haves:
  truths:
    - "machine.folderPaths persists across app restarts with no data loss for existing machines (backward-compatible fromJson)"
    - "User can add a folder path in the edit screen, reorder it, and delete it"
    - "Saving the machine writes folderPaths to shared_preferences inside machines_v1"
    - "Existing machines without a folderPaths key in stored JSON load without error and default to empty list"
  artifacts:
    - path: "lib/core/models/machine.dart"
      provides: "Machine model with folderPaths field, copyWith, fromJson (backward-compat), toJson"
      contains: "folderPaths"
    - path: "lib/features/machines/screens/add_edit_machine_screen.dart"
      provides: "Component A: folder path editor (ReorderableListView, add field, delete)"
      contains: "ReorderableListView"
  key_links:
    - from: "lib/features/machines/screens/add_edit_machine_screen.dart"
      to: "lib/core/models/machine.dart"
      via: "Machine constructor call in _save()"
      pattern: "folderPaths: _folderPaths"
    - from: "lib/core/models/machine.dart"
      to: "shared_preferences via MachineRepository"
      via: "toJson() / fromJson()"
      pattern: "'folderPaths': folderPaths"
---

<objective>
Extend the Machine model with a `folderPaths: List<String>` field and wire it into the AddEditMachineScreen as Component A (folder path editor).

Purpose: PICK-02 requires users to configure per-machine working folder paths. This plan establishes the data layer and the editor UI. No picker sheet yet — that is Plan 02.

Output:
- `machine.dart` with `folderPaths` field, full backward-compatible serialization
- `add_edit_machine_screen.dart` with Component A inserted between Password field and Save button
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-session-start-picker/06-RESEARCH.md
@.planning/phases/06-session-start-picker/06-PATTERNS.md
@.planning/phases/06-session-start-picker/06-UI-SPEC.md

<interfaces>
<!-- Exact current state of source files. Executor MUST read these before touching anything. -->

From lib/core/models/machine.dart (current, full file — 63 lines):
  - Fields: id, name, host, port, username (all required, no folderPaths yet)
  - Machine.generate() factory: no folderPaths param
  - copyWith(): no folderPaths param
  - fromJson(): no folderPaths key
  - toJson(): no folderPaths key

From lib/features/machines/screens/add_edit_machine_screen.dart:
  - State fields: _nameCtrl, _hostCtrl, _portCtrl, _usernameCtrl, _passwordCtrl, _obscurePassword, _loaded
  - dispose(): disposes all 5 controllers
  - _loadExistingMachine(): loads machine fields from machineProvider.notifier.get()
  - _save(): constructs Machine with explicit field list, calls machineProvider.notifier.save()
  - build(): SingleChildScrollView > Padding(16) > Form > Column with fields in order:
      Name → Host → Port → Username → Password → SizedBox(height:24) → FilledButton('Save Machine')
  - No folderPaths anywhere yet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Machine model with folderPaths</name>
  <files>lib/core/models/machine.dart</files>
  <read_first>
    lib/core/models/machine.dart — read the complete file before any edit; the exact field order, constructor style, and JSON serialization must be replicated exactly
  </read_first>
  <action>
    Add `folderPaths: List<String>` to the Machine class with these exact changes:

    1. Add field after `username`: `final List<String> folderPaths;`

    2. Add to primary constructor as optional named param with default:
       `this.folderPaths = const [],`
       (after `required this.username` — no `required` annotation, default is const [])

    3. Add to Machine.generate() factory — add optional named param `List<String> folderPaths = const []`
       and pass it to the `Machine(...)` constructor as `folderPaths: folderPaths`.

    4. Add to copyWith() — add `List<String>? folderPaths,` to the param list and
       `folderPaths: folderPaths ?? this.folderPaths,` to the constructor call.
       Follow the exact null-coalescing pattern used by name/host/port/username.

    5. Add to fromJson() — append after the username line:
       `folderPaths: (json['folderPaths'] as List<dynamic>?)?.cast<String>() ?? const [],`
       This handles backward compat: existing stored machines have no 'folderPaths' key,
       the cast returns null, and the default `const []` is used.

    6. Add to toJson() — append to the map literal:
       `'folderPaths': folderPaths,`

    Do NOT reformat any existing lines. Do NOT change field order for existing fields.
    Do NOT add any import — this file has no imports and must stay import-free.
  </action>
  <verify>
    grep -c 'folderPaths' lib/core/models/machine.dart
    Expected output: 7 (field decl, constructor param, generate param, generate body, copyWith param, copyWith body, fromJson, toJson = actually 8 occurrences — accept any value >= 7)
    flutter analyze lib/core/models/machine.dart — no errors
  </verify>
  <acceptance_criteria>
    - `grep 'final List<String> folderPaths' lib/core/models/machine.dart` returns one match
    - `grep "folderPaths = const \[\]" lib/core/models/machine.dart` returns at least one match (constructor default)
    - `grep "List<dynamic>.*cast.*String.*?? const \[\]" lib/core/models/machine.dart` returns one match (backward-compat fromJson)
    - `grep "'folderPaths': folderPaths" lib/core/models/machine.dart` returns one match (toJson)
    - `flutter analyze lib/core/models/machine.dart` exits 0 with no errors
    - Existing machines (no folderPaths key in stored JSON) must deserialize without null error: `Machine.fromJson({'id':'x','name':'y','host':'z','port':22,'username':'u'}).folderPaths` == []
  </acceptance_criteria>
  <done>Machine model has folderPaths field with backward-compatible JSON serialization and no analysis errors</done>
</task>

<task type="auto">
  <name>Task 2: Add Component A folder editor to AddEditMachineScreen</name>
  <files>lib/features/machines/screens/add_edit_machine_screen.dart</files>
  <read_first>
    lib/features/machines/screens/add_edit_machine_screen.dart — read the complete file before any edit; the insertion points for new state fields, dispose(), _loadExistingMachine(), _save(), and the form Column children are exact line numbers that must be respected
    lib/features/machines/screens/add_edit_machine_screen.dart — note the existing TextFormField pattern (OutlineInputBorder, validator style) to replicate for the add-path field
    .planning/phases/06-session-start-picker/06-UI-SPEC.md — Component A section for exact copy, spacing, and semantics specs
  </read_first>
  <action>
    Extend _AddEditMachineScreenState with a folder path editor (Component A). Make these targeted changes:

    STATE FIELDS — add after `bool _loaded = false;`:
      `final _folderPathCtrl = TextEditingController();`
      `List<String> _folderPaths = [];`

    DISPOSE — add before `super.dispose();`:
      `_folderPathCtrl.dispose();`

    _loadExistingMachine() — add after `_loaded = true;` (but BEFORE it, inside the null-guard block,
    after all existing field loads) so it runs only when a machine exists:
      `_folderPaths = List<String>.from(machine.folderPaths);`
    Note: machine.folderPaths is now available from Task 1.

    _SAVE() — update the Machine(...) constructor call to add:
      `folderPaths: _folderPaths,`
    after `username: _usernameCtrl.text.trim(),` — all other fields unchanged.

    FORM COLUMN — replace the existing `const SizedBox(height: 24),` that appears just before the
    FilledButton with this sequence:
      const SizedBox(height: 16),
      Divider(),
      const SizedBox(height: 8),
      Text('Working folders', style: Theme.of(context).textTheme.bodyMedium?.copyWith(fontWeight: FontWeight.w600, color: Theme.of(context).colorScheme.onSurfaceVariant)),
      const SizedBox(height: 8),
      [if _folderPaths.isEmpty: empty-state Text widget, else: ReorderableListView block],
      const SizedBox(height: 8),
      [add-path row: TextFormField + add button],
      const SizedBox(height: 24),
      [existing FilledButton unchanged]

    EMPTY STATE widget (shown when _folderPaths is empty):
      Padding(
        padding: const EdgeInsets.symmetric(vertical: 16),
        child: Text('No folders configured. Add a path to enable the session picker.',
          textAlign: TextAlign.center,
          style: Theme.of(context).textTheme.labelSmall?.copyWith(color: Theme.of(context).colorScheme.onSurfaceVariant),
        ),
      )

    REORDERABLELISTVIEW block (shown when _folderPaths is not empty):
      ReorderableListView(
        shrinkWrap: true,
        physics: const NeverScrollableScrollPhysics(),
        onReorder: (oldIndex, newIndex) {
          setState(() {
            if (newIndex > oldIndex) newIndex--;
            final item = _folderPaths.removeAt(oldIndex);
            _folderPaths.insert(newIndex, item);
          });
        },
        children: [
          for (int i = 0; i < _folderPaths.length; i++)
            Padding(
              key: ValueKey('folder_$i'),
              padding: const EdgeInsets.only(bottom: 8),
              child: Container(
                decoration: BoxDecoration(
                  color: colorScheme.surfaceContainerHighest,
                  borderRadius: BorderRadius.circular(8),
                ),
                child: ListTile(
                  leading: Semantics(label: 'Reorder', child: const Icon(Icons.drag_handle)),
                  title: Text(_folderPaths[i], style: const TextStyle(fontSize: 14)),
                  trailing: Semantics(
                    label: 'Remove folder path',
                    child: IconButton(
                      icon: Icon(Icons.delete, color: colorScheme.error),
                      onPressed: () => setState(() => _folderPaths.removeAt(i)),
                    ),
                  ),
                ),
              ),
            ),
        ],
      )

    ADD-PATH ROW (always shown below the list or empty state):
      Row(
        children: [
          Expanded(
            child: TextFormField(
              controller: _folderPathCtrl,
              decoration: const InputDecoration(
                labelText: 'Folder path',
                hintText: '/home/user/projects/myapp',
                border: OutlineInputBorder(),
              ),
              textInputAction: TextInputAction.done,
              onFieldSubmitted: (_) => _addFolderPath(),
            ),
          ),
          const SizedBox(width: 8),
          Semantics(
            label: 'Add folder path',
            child: IconButton(
              icon: const Icon(Icons.add),
              onPressed: _addFolderPath,
            ),
          ),
        ],
      )

    ADD _addFolderPath() helper method to the State class (before build):
      void _addFolderPath() {
        final path = _folderPathCtrl.text.trim();
        if (path.isEmpty) return;
        setState(() {
          _folderPaths.add(path);
          _folderPathCtrl.clear();
        });
      }

    KEY RULE: Use index-based ValueKey('folder_$i') on each Padding in the
    ReorderableListView children — NOT ValueKey(path) — to handle duplicate paths safely (Pitfall 4 in RESEARCH.md).

    Declare `final colorScheme = Theme.of(context).colorScheme;` at the top of build() (or reuse if already present — check first).

    Do NOT add a validator to the folder path TextFormField (field is optional; empty paths silently ignored by _addFolderPath).
    Do NOT use a required annotation on folderPaths in Machine (Task 1 gives it a default).
    Do NOT use isScrollControlled on showModalBottomSheet here (that is Plan 02).
  </action>
  <verify>
    flutter analyze lib/features/machines/screens/add_edit_machine_screen.dart
    Expected: no errors
    grep -c 'ReorderableListView' lib/features/machines/screens/add_edit_machine_screen.dart
    Expected: >= 1
    grep -c '_folderPaths' lib/features/machines/screens/add_edit_machine_screen.dart
    Expected: >= 5 (field decl, load, save, reorder, delete, add)
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/features/machines/screens/add_edit_machine_screen.dart` exits 0, no errors
    - `grep 'ReorderableListView' lib/features/machines/screens/add_edit_machine_screen.dart` returns at least 1 match
    - `grep "folderPaths: _folderPaths" lib/features/machines/screens/add_edit_machine_screen.dart` returns 1 match (in _save())
    - `grep "_folderPaths = List<String>.from" lib/features/machines/screens/add_edit_machine_screen.dart` returns 1 match (in _loadExistingMachine())
    - `grep "_folderPathCtrl.dispose" lib/features/machines/screens/add_edit_machine_screen.dart` returns 1 match
    - `grep "ValueKey('folder_" lib/features/machines/screens/add_edit_machine_screen.dart` returns at least 1 match (index-based keys)
    - `grep "shrinkWrap: true" lib/features/machines/screens/add_edit_machine_screen.dart` returns 1 match (prevents unbounded height error)
    - `grep "NeverScrollableScrollPhysics" lib/features/machines/screens/add_edit_machine_screen.dart` returns 1 match
    - `grep "'No folders configured" lib/features/machines/screens/add_edit_machine_screen.dart` returns 1 match (empty state)
    - `flutter analyze .` exits 0 with no errors across the whole project
  </acceptance_criteria>
  <done>Component A is fully wired: adding, reordering, deleting folder paths works in the edit screen, folderPaths is saved and loaded correctly, and the whole project has no analysis errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Machine model | Folder path strings entered by user; stored locally, sent as `cd` argument to SSH shell in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Machine.fromJson (shared_preferences) | accept | Data is local device storage; no external write path; existing flutter_secure_storage protects credentials; folder paths are non-sensitive metadata |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 6 — package legitimacy gate not applicable (verified in RESEARCH.md § Package Legitimacy Audit) |
</threat_model>

<verification>
1. `flutter analyze .` — zero errors across entire project
2. `grep -c 'folderPaths' lib/core/models/machine.dart` — at least 7 occurrences
3. Manual: Open the edit screen for an existing machine — all existing fields load correctly, folderPaths section shows empty state
4. Manual: Add a path, save, reopen machine — path is present
5. Manual: Reorder paths, save, reopen — order preserved
6. Manual: Delete a path, save, reopen — path gone
</verification>

<success_criteria>
- Machine model has folderPaths field with backward-compatible fromJson (no crash on existing machines)
- Component A renders between Password field and Save button: section header, empty state or list, add-path row
- ReorderableListView uses index-based ValueKey('folder_$i') — duplicate paths do not crash
- _folderPathCtrl is disposed; _addFolderPath ignores empty input silently
- All project files pass `flutter analyze .` with no errors
- PICK-02 satisfied: user can add, reorder, delete folder paths per machine
</success_criteria>

<output>
Create `.planning/phases/06-session-start-picker/06-01-SUMMARY.md` when done
</output>
